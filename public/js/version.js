/**
 * Die Fassung dieser App.
 *
 * Der Wert wird nicht von Hand gepflegt, sondern aus dem Inhalt aller
 * ausgelieferten Dateien gerechnet (scripts/version.mjs). Ein Test rechnet
 * nach und wird rot, wenn der Stempel nicht mehr passt - so kann niemand
 * vergessen, ihn hochzuzählen, und niemand bleibt auf einer alten Fassung
 * sitzen, ohne es zu merken.
 */
export const APP_VERSION = 'a5a175cda8e2';
